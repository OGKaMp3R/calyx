console.error("npm run preview is intentionally blocked.");
console.error("Use `npm run stable` for the trusted app runtime.");
console.error("Use `npm run preview:dist-only` only for temporary static inspection.");
process.exitCode = 1;
